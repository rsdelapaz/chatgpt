/* Minimal JavaScript for mobile menu toggling (optional) */

// Example: toggle submenu on touch devices (if desired)
// For a more advanced solution, you might implement a hamburger menu.

document.addEventListener('DOMContentLoaded', () => {
  // For this landing page, no advanced JavaScript is required.
  // You can add mobile menu toggling logic here if needed.
});
